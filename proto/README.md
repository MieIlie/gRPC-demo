# Proto Directory

This directory will store all the Protocol Buffer definitions (`.proto` files) for the gRPC services.

* Centralized contracts: `/proto`
* Generated output: `/gen` (under services and shared modules)
